import Image from "next/image";

export default function Home() {
  return (
    <main className="font-home flex min-h-screen flex-col items-center justify-between p-24 bg-bg1 bg-cover bg-center bg-gradient-overlay">
      <nav></nav>
    </main>
  );
}
